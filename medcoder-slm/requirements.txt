# MedCoder-SLM-1B — Requirements
# Python 3.10+  |  CUDA 12.1+  |  8GB+ VRAM (T4 minimum)
#
# Install: pip install -r requirements.txt

# Core training stack
torch>=2.2.0
transformers>=4.44.0
datasets>=2.18.0
accelerate>=0.27.0
peft>=0.10.0
trl>=0.8.1
bitsandbytes>=0.43.0     # 4-bit quantization

# HuggingFace upload
huggingface_hub>=0.22.0

# Data processing
pandas>=2.0.0

# REST API (optional — only needed for inference.py --serve)
fastapi>=0.110.0
uvicorn>=0.28.0
pydantic>=2.0.0

# Evaluation utilities
tqdm>=4.66.0
scikit-learn>=1.4.0

# Notebook support (optional)
jupyter>=1.0.0
ipywidgets>=8.1.0
